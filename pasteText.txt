Some random reference from searches.

#checking if the created table exists
#res = dbCursor.execute("SELECT name FROM sqlite_master")
#print(res.fetchone())

# data = [
#     ("Watch One Piece", 0),
#     ("Play Abiotic Factor", 1),
#     ("Go bowling", 0),
# ]
# dbCursor.executemany("INSERT INTO tasks VALUES(?, ?)", data)
# dbConn.commit()  # Remember to commit the transaction after executing INSERT.